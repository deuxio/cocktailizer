<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @cocktail.name %>
</p>

<p>
  <strong>Recettes:</strong>
  <% @cocktail.doses.each do |dose| %>
  <p><%= dose.amount %> de <%= dose.ingredient.name %><p>
  <% end %>
</p>

<%= link_to 'Edit', edit_cocktail_path(@cocktail) %> |
<%= link_to 'Back', cocktails_path %>
