<%= form_for([@cocktail, @dose]) do |f| %>
  <% if @dose.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dose.errors.count, "error") %> prohibited this dose from being saved:</h2>

      <ul>
      <% @dose.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :amount %> cl. of <%= f.select :ingredient_id, @ingredients.map {|ingredient| [ ingredient.name, ingredient.id ]} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>



